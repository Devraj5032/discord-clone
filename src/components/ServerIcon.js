import React from 'react'

function ServerIcon(props) {
  return (
    <img src={props.image} alt='' className='h-12 cursor-pointer rounded-full transition-all duration-100 ease-in-out hover:rounded-2xl'/>
  )
}

export default ServerIcon